<%init>
use lib $ENV{DOCUMENT_ROOT};
use Shim::Tools;
my %post_content = Shim::Tools::post_content('summary');
my $post = Shim::Post->new('summary');
</%init>
<& '/logged-in/new-post', title=>$post_content{title}, body=>(join "", @{$post_content{body}}), id=>'summary', tags=>[$post->tags], &>
<%method title>Edit Summary</%method>
